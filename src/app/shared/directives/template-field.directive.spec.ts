import { TemplateFieldDirective } from './template-field.directive';

describe('TemplateFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateFieldDirective();
    expect(directive).toBeTruthy();
  });
});
